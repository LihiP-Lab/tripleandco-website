{
  "name": "tripleandco-qa",
  "private": true,
  "type": "module",
  "description": "QA suite for tripleandco.com. Kept out of the app's package.json so Vercel builds stay untouched.",
  "scripts": {
    "test": "playwright test tests/routes.spec.ts",
    "a11y": "playwright test tests/a11y.spec.ts --project=desktop",
    "rules": "node rules.mjs",
    "seo": "node aggregate.mjs",
    "all": "npm run rules && npm run test && npm run seo && npm run a11y",
    "report": "playwright show-report results/html-report"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.56.0",
    "axe-core": "^4.10.3"
  }
}
